import React from 'react';
import { StyleSheet, View, Text ,Image, FlatList} from 'react-native';
import Header from '../shared/headerNotif';

export default function Notif({ navigation }) {
    return(
        <View>
        <Header/>
        

        </View>
       
    )
}
const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'stretch'
    }})